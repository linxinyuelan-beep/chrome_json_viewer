export const DRAWER_UI = {
  MIN_WIDTH_PX: 300,
  MAX_WIDTH_PX: 1600,
  MAX_VIEWPORT_RATIO: 0.9,
} as const;

export const NOTIFICATION_UI = {
  DURATION_MS: 3000,
  FADE_MS: 500,
  Z_INDEX: '100000',
} as const;

export const DETECTION_UI = {
  HOVER_THROTTLE_MS: 150,
  LOAD_INIT_DELAY_MS: 500,
} as const;

export const WINDOW_UI = {
  JSON_WINDOW_WIDTH: 1024,
  JSON_WINDOW_HEIGHT: 768,
} as const;

export const HISTORY_UI = {
  MAX_ITEMS: 50,
  PREVIEW_LENGTH: 50,
} as const;

